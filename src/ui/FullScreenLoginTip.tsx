import React from 'react';

export default function FullScreenLoginTip() {
  return (
    <div style={{
      height: '100%',
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
    }}>
      请登录
    </div>
  );
}
